# watch-anchors

Head hashes of the Thalor Log, one commit per snapshot: seq, entry_hash, as_of, computed_at_utc, anchored_at_utc. Nothing else. The commit timestamp is the anchor. Verify against https://watch.thalorworks.com/verify
